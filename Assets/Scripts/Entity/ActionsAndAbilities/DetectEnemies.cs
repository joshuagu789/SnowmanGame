/*
 * Squad ability mainly intended for Model B to report nearby enemies to player but might allow other entities to use it in the future
 */
/*
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DetectEnemies : MonoBehaviour
{
    [SerializeField]
    private Compass compass;

    public Dictionary<string, float> GetLocations(MonoBehaviour target)
    {
        Dictionary<string, float> locations = new Dictionary<string, float>();

        if (CanUseAbility())
        {
            ExpendEnergy();
            ResetCooldown();

        }
        return locations;
    }
}
*/
